//code chef problem:if condition in C
#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);

    if (n > 5) {
        printf("the number is greater than 5");
    } else {
        printf("the number is smaller than or equal to 5");
    }

    return 0;
}
